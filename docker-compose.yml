services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      PORT: 3001
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
